# -*- python -*-
from lsst.sconsUtils import scripts
scripts.BasicSConstruct("obs_decam", defaultTargets=scripts.DEFAULT_TARGETS + ("decam",),
                        disableCc=True, subDirList=['bin', 'config', 'doc', 'pipelines', 'python', 'ups'])
